<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}FoodMate{% endblock %}</title>
    <meta name="description"
          content="Aplikacja ma pomóc użytkownikowi w organizowaniu posiłków, planowaniu menu, robieniu listy zakupów oraz śledzeniu zawartości lodówki.">
    <link href="{{ asset('https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css') }}" rel="stylesheet">

    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}

    <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}"/>

</head>
<body id="{% block body_id %}{% endblock %}">
{% block body %}
    {% block header %}
        {% set _route = app.request.get('_route') %}
        <div class="container-fluid">
            <div class="d-flex align-items-center justify-content-between">
                <a class="navbar-brand me-2" href="{{ path('homepage') }}">
                    <img src="{{ asset('images/logo.png') }}" />
                </a>
                <div class="d-flex align-items-center">
                    <a class="btn btn-success" href="{{ path('app_login') }}" role="button">Zaloguj&nbsp;się</a>
                    <a class="btn btn-success ms-3" href="{{ path('app_login') }}" role="button">Rejestracja</a>
                </div>
            </div>
        </div>
        {{ include('default/_navigation.html.twig') }}
    {% endblock %}

    <div class="container body-container">
        <div class="row">
            <div id="main" class="col-sm-12">
                {{ include('default/_flash_messages.html.twig') }}

                {% block main %}{% endblock %}
            </div>

            {#            <div id="sidebar" class="col-sm-3"> #}
            {#                {% block sidebar %} #}
            {#                    SideBar #}
            {#                {% endblock %} #}
            {#            </div> #}
        </div>
    </div>
{% endblock %}
<script src="{{ asset('https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js') }}"></script>
<script src="{{ asset('https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js') }}"></script></body>
</html>
